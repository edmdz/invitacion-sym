---
// --- La parte superior (script y HTML) no cambia ---
const registry = {
  storeName: "Liverpool",
  logoSrc: "/liverpool.png",
  eventNumber: "51747839",
  registryUrl: "https://mesaderegalos.liverpool.com.mx/milistaderegalos/51747839"
};
---

<section class="gift-section">
  <div class="header">
    {/* <img src="/img/adorno-hojas.svg" alt="Adorno de hojas" class="adorno" /> */}
    <h2>MESA DE REGALOS</h2>
    <p>
      El mejor regalo es tu presencia, pero si deseas hacernos llegar un bonito
      detalle lo recibiremos con mucho cariño.
    </p>
  </div>

  <div class="registry-card">
    <img src={registry.logoSrc} alt={`Logo de ${registry.storeName}`} class="store-logo" />
    <p class="event-number">NO. DE EVENTO: {registry.eventNumber}</p>
    <a href={registry.registryUrl} target="_blank" rel="noopener noreferrer" class="registry-button">
      VER MESA DE REGALOS
    </a>
  </div>
</section>

{/* 👇 REEMPLAZA TU SECCIÓN DE ESTILOS CON ESTA 👇 */}
<style>
  .gift-section {
    background-color: var(--soft-white);
    font-family: var(--font-serif);
    color: var(--ink);
    padding: clamp(40px, 10vw, 80px) 20px;
    text-align: center;
  }

  .header {
    max-width: 550px;
    margin: 0 auto 40px auto;
  }

  .adorno {
    width: 80px;
    height: auto;
    margin-bottom: 20px;
    color: var(--gold);
  }

  .header h2 {
    font-size: clamp(1.5rem, 5vw, 1.8rem);
    font-weight: 600;
    letter-spacing: 0.1em;
    margin: 0 0 20px;
    color: var(--gold); /* <-- CAMBIO: Título dorado */
  }

  .header p {
    font-family: var(--font-sans);
    font-size: clamp(1rem, 3vw, 1.1rem);
    line-height: 1.6;
    max-width: 45ch;
    margin: 0 auto;
  }

  .registry-card {
    background-color: var(--white);
    max-width: 450px;
    margin: 0 auto;
    padding: 40px 30px;
    border-radius: 4px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
  }

  .store-logo {
    max-width: 200px;
    height: auto;
    margin-bottom: 25px;
    filter: brightness(0) saturate(100%);
  }

  .event-number {
    font-family: var(--font-sans);
    font-weight: 600;
    letter-spacing: 0.1em;
    font-size: 0.9rem;
    margin: 0 0 25px;
  }

  .registry-button {
    background-color: var(--sand); /* <-- CAMBIO: Fondo arena */
    color: var(--ink);             /* <-- CAMBIO: Texto oscuro */
    font-family: var(--font-sans);
    display: inline-block;
    padding: 14px 30px;
    border-radius: 50px;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.8rem;
    letter-spacing: 0.15em;
    border: none;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.2s ease;
  }

  .registry-button:hover {
    background-color: color-mix(in srgb, var(--sand) 85%, black);
    transform: translateY(-2px);
  }
</style>